{
  "name": "dzifa-bakes",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./api\" --filter \"./web\" --if-present run typecheck",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db run push",
    "db:push-force": "dotenv -e .env -- pnpm --filter @workspace/db run push-force"
  },
  "private": true,
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
